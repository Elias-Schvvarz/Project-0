[project]
name = "project-0"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "elias", email = "eliaschvvarz@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "more-itertools>=11.1.0",
]

[project.scripts]
project-0 = "project_0:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ruff>=0.16.2",
]
